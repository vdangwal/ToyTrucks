apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog_api_claim0
  labels:
    app: catalog_api_claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
