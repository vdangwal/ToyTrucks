apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: discount_grpc_claim0
  labels:
    app: discount_grpc_claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
