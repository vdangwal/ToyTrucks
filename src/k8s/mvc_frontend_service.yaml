apiVersion: v1
kind: Service
metadata:
  name: mvc_frontend
  labels:
    app: mvc_frontend
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 80
    - name: "8001"
      port: 8001
      targetPort: 443
  selector:
    app: mvc_frontend
