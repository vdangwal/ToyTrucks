apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mvc_frontend_claim0
  labels:
    app: mvc_frontend_claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
